#ifndef MACHINE_TESTS_RUNNER_H
#define MACHINE_TESTS_RUNNER_H

/* Run all machine tests. speed_mhz: 0 = hyperspeed. Returns 0 if all pass, 1 if any fail. */
int run_all_machine_tests(double speed_mhz);

#endif /* MACHINE_TESTS_RUNNER_H */
